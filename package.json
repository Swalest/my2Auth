{
  "name": "saw2auth",
  "version": "1.0.0",
  "description": "the authetification and role-based authorization",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rimraf dist && tsc",
    "prepublish": "npm run build"
  },
  "author": "Swalest",
  "license": "ISC",
  "dependencies": {
    "@nestjs/common": "^10.2.8",
    "@nestjs/core": "^10.2.8",
    "@nestjs/jwt": "^10.1.1",
    "@types/node": "^20.9.0",
    "dotenv": "^16.3.1",
    "rimraf": "^5.0.5",
    "rxjs": "^7.8.1",
    "typescript": "^5.2.2"
  }
}
